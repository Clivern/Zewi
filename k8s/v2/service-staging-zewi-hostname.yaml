apiVersion: v1
kind: Service
metadata:
  name: zewi-hostname
  namespace: staging
  labels:
    app: zewi-hostname
spec:
  type: ClusterIP
  ports:
  - port: 8003
    targetPort: 8003
    protocol: TCP
    name: http
  selector:
    app: zewi-hostname
