apiVersion: v1
kind: Service
metadata:
  name: zewi-landing
  namespace: staging
  labels:
    app: zewi-landing
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: zewi-landing
